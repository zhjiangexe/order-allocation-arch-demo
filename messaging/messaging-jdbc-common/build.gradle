plugins {
    id 'java-library'
}

description = 'framework-neutral JDBC and transaction ports for messaging adapters'

dependencies {
    testImplementation libs.spring.boot.starter.test
    testRuntimeOnly libs.junit.platform.launcher
}
