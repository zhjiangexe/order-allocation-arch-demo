# e2e/spec

Karate `.feature` correctness tests against the same real infra as `../perf`
(PostgreSQL + Kafka + Kafka Connect/Debezium, app running on host) — asserting
end-to-end behavior instead of load-generating it.

Not built yet.
