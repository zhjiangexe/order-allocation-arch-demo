plugins {
    id 'java-library'
}

description = 'framework-neutral generic message producer orchestration'

dependencies {
    api project(':messaging:messaging-api')

    testImplementation libs.spring.boot.starter.test
    testRuntimeOnly libs.junit.platform.launcher
}
