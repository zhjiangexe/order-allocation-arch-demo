plugins {
    id 'java-library'
}

description = 'narrow Spring Boot starter for idempotent programmatic Kafka consumers'

dependencies {
    api project(':archone-framework:messaging:messaging-spring-boot-autoconfigure')
    api project(':archone-framework:messaging:messaging-events')
    api project(':archone-framework:messaging:messaging-consumer-common')
    api project(':archone-framework:messaging:messaging-consumer-jdbc')
    api project(':archone-framework:messaging:messaging-consumer-kafka')
    api project(':archone-framework:messaging:messaging-spring-jdbc')
    api project(':archone-framework:messaging:messaging-spring-consumer-jdbc')
    api project(':archone-framework:messaging:messaging-spring-consumer-kafka')
    api project(':archone-framework:messaging:messaging-spring-observability')
    api project(':archone-framework:messaging:messaging-spring-consumer-observability')
    api libs.spring.boot.starter.jdbc
    api libs.spring.boot.starter.kafka
    api libs.jackson.databind

    testImplementation libs.spring.boot.starter.test
    testRuntimeOnly libs.junit.platform.launcher
}
