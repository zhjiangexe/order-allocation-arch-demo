plugins {
    id 'java-library'
}

description = 'Temporal fulfillment workflow and activity contracts'

dependencies {
    api libs.temporal.sdk

    testImplementation libs.spring.boot.starter.test
    testRuntimeOnly libs.junit.platform.launcher
}
