package com.flowzati.archone.inventory.movement.domain;

/** Whether the caller still needs Inventory to obtain the warehouse cancellation checkpoint. */
public enum WarehouseCancellationCheckpoint {
    REQUIRED,
    CONFIRMED
}
