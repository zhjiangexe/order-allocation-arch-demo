package com.flowzati.archone.inventory.movement.domain.policy;

/** Policy applied atomically to all confirmed moves in one stock operation. */
public enum MovementAssignmentPolicy {
    SHIP_COMPLETE
}
